diff --git a/filesystem_utils.h b/filesystem_utils.h
index fdf172c..0728ae2 100644
--- a/filesystem_utils.h
+++ b/filesystem_utils.h
@@ -169,7 +169,7 @@ static path_t sanitize_filepath(const path_t& path)
     if (filepath_is_readable(path))
         return path;
 
-    return get_executable_directory() + path;
+    return get_executable_directory() + "../share/models/" + path;
 }
 
 #endif // FILESYSTEM_UTILS_H
