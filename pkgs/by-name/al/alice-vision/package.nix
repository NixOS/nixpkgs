{
  lib,
  stdenv,
  fetchFromGitHub,
  testers,
  nix-update-script,
  cmake,
  pkg-config,

  assimp,
  # use the same boost as in cctag here
  boost186,
  ceres-solver,
  clp,
  coin-utils,
  eigen,
  expat,
  flann,
  geogram,
  lemon-graph,
  libe57format,
  lz4,
  nanoflann,
  openexr,
  openimageio,
  osi,
  suitesparse,
  zlib,

  config,
  cudaSupport ? config.cudaSupport,
  cudaPackages ? { },

  enableOpenMP ? true,
  # Separate openmp input only required on Darwin stdenv (LLVM)
  llvmPackages,

  enableAlembic ? true,
  alembic,
  enableApriltag ? true,
  apriltag,
  enableCctag ? true,
  cctag,
  enableMeshSDFilter ? true,
  openmesh,
  enableONNX ? true,
  onnxruntime,
  enableOpenCV ? true,
  enableOpenCVContrib ? enableOpenCV,
  opencv,
  enableOpenGV ? true,
  opengv,
}:

assert lib.asserts.assertMsg (
  enableOpenCVContrib -> enableOpenCV
) "enableOpenCVContrib requires enableOpenCV";

let
  effectiveStdenv = if cudaSupport then cudaPackages.backendStdenv else stdenv;
  enableCuda = cudaSupport;
in
effectiveStdenv.mkDerivation (finalAttrs: {
  pname = "alice-vision";
  version = "3.3.0";

  outputs = [
    "out"
    "dev"
  ];

  src = fetchFromGitHub {
    owner = "alicevision";
    repo = "AliceVision";
    tag = "v${finalAttrs.version}";
    hash = "sha256-eG6cZ1sDsWE8aegnY0QY4cnbVnv0vPPTSFwMeujqLeM=";
  };

  nativeBuildInputs = [
    cmake
    pkg-config
  ]
  ++ lib.optional enableCuda cudaPackages.cuda_nvcc;

  buildInputs = [
    flann
    libe57format
    nanoflann
    openexr

    # Temporary fix until flann 1.9.2 is in Nixpkgs
    lz4
  ]
  ++ lib.optional enableCuda cudaPackages.cuda_cudart;

  propagatedBuildInputs = [
    assimp
    boost186
    ceres-solver
    clp
    coin-utils
    eigen
    expat
    geogram
    lemon-graph
    openimageio
    osi
    suitesparse
    zlib
  ]
  ++ lib.optional enableAlembic alembic
  ++ lib.optional enableApriltag apriltag
  ++ lib.optional enableCctag cctag
  ++ lib.optional enableMeshSDFilter openmesh
  ++ lib.optional enableONNX onnxruntime
  ++ lib.optional enableOpenCV opencv
  ++ lib.optional enableOpenGV opengv
  ++ lib.optional enablePopsift popsift
  ++ lib.optional stdenv.cc.isClang llvmPackages.openmp;

  patches = [
    # Don't optimize for the host processor (could lead to reproducibility issues)
    ./cmake-disable-optimize-for-architecture.patch

    # Upstream uses patched Clp/Osi/CoinUtils containing CMake build scripts.
    # Instead of the patched versions, we re-use our packages and add CMake
    # find modules that use the pkg-config files already generated by those
    # dependencies.
    ./find-coin-modules.patch
    ./find-flann.patch

    # AliceVision expects OpenImageIO v2
    ./openimageio-3-compat.patch

    ./cmake-fix-disabled-cuda-build.patch
  ];

  cmakeFlags =
    let
      # alice-vision uses a tri-state bool type incompatible with lib.cmakeBool
      cmakeTriBool = name: enabled: "-D${name}:BOOL=" + (if enabled then "ON" else "OFF");
    in
    lib.mapAttrsToList
      (name: cmakeTriBool "ALICEVISION_USE_${lib.toUpper (lib.removePrefix "enable" name)}")
      {
        inherit
          enableAlembic
          enableApriltag
          enableCctag
          enableCuda
          enableMeshSDFilter
          enableONNX
          enableOpenCV
          enableOpenMP
          enableOpenGV
          ;

        enableOpenCV_Contrib = enableOpenCVContrib;

        # Could be enabled, but does not compile as of v3.3.0 (upstream bug)
        enableOcvsift = enableOpenCVContrib && false;

        # dependencies not packaged at the moment
        enablePopsift = false;
        enableUncertaintyTE = false;
        enableUSD = false;
      }
    ++ lib.mapAttrsToList lib.cmakeBool {
      ALICEVISION_BUILD_DEPENDENCIES = false;

      # don't build executables
      ALICEVISION_BUILD_SOFTWARE = false;

      ALICEVISION_BUILD_TESTS = finalAttrs.finalPackage.doCheck;

      # CCTag package does not support alignment yet, and this must be
      # consistent between both packages
      AV_EIGEN_MEMORY_ALIGNMENT = !enableCctag;

      ALICEVISION_REQUIRE_CERES_WITH_SUITESPARSE = true;
    }
    ++ lib.mapAttrsToList (lib.cmakeOptionType "PATH") {
      CMAKE_MODULE_PATH = "${./cmake-modules}";
      FIND_COIN_MODULE_PATH = "${./cmake-modules}";
    };

  postInstall = ''
    mkdir -p $dev/share/aliceVision
    mv $out/share/aliceVision/cmake $dev/share/aliceVision

    mkdir -p $dev/include/aliceVision
    mv $out/include/aliceVision/* $dev/include/aliceVision/
    mv $out/include/aliceVision_dependencies $dev/include
    rmdir $out/include/aliceVision
    rmdir $out/include
  '';

  doCheck = true;

  checkPhase =
    let
      disabledTests = [
        # Tests that can take a long time (>30 seconds) to run
        "^test_aliceVision_test_sfm_panorama_(radial3|equidistant)(_outliers)?$"
        "^test_aliceVision_test_sfm_bundleAdjustment_Enhanced$"
        "^test_aliceVision_test_hdr_(debevec|laguerre|grossberg)$"
        "^test_aliceVision_test_voctree_kmeans$"
      ]
      ++ lib.optionals stdenv.isDarwin [
        # Regular timeouts
        "^test_aliceVision_test_colorHarmonization_gainOffsetConstraintBuilder$"
        "^test_aliceVision_test_image$"
        "^test_aliceVision_test_features$"
        "^test_aliceVision_test_robustEstimation_acRansac$"

        # Fails on Darwin
        "^test_aliceVision_test_voctree_vocabularyTree$"
      ];
      excludeRegex = lib.concatStringsSep "|" disabledTests;
      exclude = "--exclude-regex ${lib.escapeShellArg excludeRegex}";
    in
    ''
      runHook preCheck

      ${lib.optionalString stdenv.isDarwin ''
        export DYLD_LIBRARY_PATH="${lib.getLib geogram}/lib:$(pwd)"
      ''}

      ctest \
          --force-new-ctest-process \
          --timeout 180 \
          ${exclude}

      runHook postCheck
    '';

  passthru = {
    tests.cmake = testers.hasCmakeConfigModules {
      package = finalAttrs.finalPackage;
      moduleNames = [ "AliceVision" ];
    };
    updateScript = nix-update-script { };
  };

  # recommended for CUDA packaging
  __structuredAttrs = true;
  strictDeps = true;

  meta = {
    description = "Photogrammetric Computer Vision Framework which provides a 3D Reconstruction and Camera Tracking algorithms";
    homepage = "https://alicevision.org";
    downloadPage = "https://github.com/alicevision/AliceVision";
    platforms = lib.platforms.all;

    # As specified in COPYING.md
    license = with lib.licenses; [
      # AliceVision itself
      mpl20
      # some parts of AliceVision (from libmv)
      mit
      # kvld
      bsd2
    ];

    maintainers = with lib.maintainers; [
      tmarkus
    ];
  };
})
