diff --git a/kalamine/template.py b/kalamine/template.py
index bdf786e..b738d5f 100644
--- a/kalamine/template.py
+++ b/kalamine/template.py
@@ -32,7 +32,7 @@ def substitute_token(text: str, token: str, value: str) -> str:
 
 
 def load_tpl(layout: "KeyboardLayout", ext: str, tpl: str = "base") -> str:
-    date = datetime.date.today().isoformat()
+    date = datetime.date.fromtimestamp(0).isoformat()
     if tpl == "base":
         if layout.has_altgr or ext.startswith(".RC"):
             tpl = "full"
diff --git a/kalamine/xkb_manager.py b/kalamine/xkb_manager.py
index 9ea8aad..6169312 100644
--- a/kalamine/xkb_manager.py
+++ b/kalamine/xkb_manager.py
@@ -36,7 +36,7 @@ XKB_ROOT = Path(environ.get("XKB_CONFIG_ROOT") or "/usr/share/X11/xkb/")
 
 WAYLAND = wayland_running()
 
-KALAMINE_MARK = f"Generated by kalamine on {datetime.date.today().isoformat()}"
+KALAMINE_MARK = f"Generated by kalamine on {datetime.date.fromtimestamp(0).isoformat()}"
 XKB_RULES_HEADER = f"""\
 <?xml version="1.0" encoding="UTF-8"?>
 <!DOCTYPE xkbConfigRegistry SYSTEM "xkb.dtd">
