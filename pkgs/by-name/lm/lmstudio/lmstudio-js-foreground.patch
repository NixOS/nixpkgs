diff --git a/packages/lms-common-server/src/findOrStartLlmster.ts b/packages/lms-common-server/src/findOrStartLlmster.ts
index 9cc4bb4..351de45 100644
--- a/packages/lms-common-server/src/findOrStartLlmster.ts
+++ b/packages/lms-common-server/src/findOrStartLlmster.ts
@@ -1,5 +1,5 @@
 import { apiServerPorts, type LoggerInterface } from "@lmstudio/lms-common";
-import { spawn } from "child_process";
+import { spawn, type ChildProcess } from "child_process";
 import { existsSync, readFileSync, writeFileSync } from "fs";
 import { join } from "path";
 import { findLMStudioHome } from "./findLMStudioHome.js";
@@ -32,6 +32,12 @@ export interface FindOrStartLlmsterOptions {
    * Interval in milliseconds between polling attempts. Defaults to 1000ms.
    */
   pollIntervalMs?: number;
+  /**
+   * When true, the daemon process is spawned in the foreground (attached, stdio inherited) instead
+   * of being detached.  The returned promise will not resolve until the daemon exits.  Useful for
+   * process supervisors that need to own the daemon lifecycle.
+   */
+  foreground?: boolean;
 }
 
 /**
@@ -44,7 +50,7 @@ export interface FindOrStartLlmsterOptions {
 export async function findOrStartLlmster(
   options: FindOrStartLlmsterOptions = {},
 ): Promise<APIServerStatus | null> {
-  const { installLlmster, maxAttempts = 60, pollIntervalMs = 1000 } = options;
+  const { installLlmster, maxAttempts = 60, pollIntervalMs = 1000, foreground = false } = options;
   const logger: LoggerInterface = (options.logger ?? console) as LoggerInterface;
 
   // 1. Try to find an already running daemon.
@@ -114,7 +120,7 @@ export async function findOrStartLlmster(
   }
 
   // 3. Start the daemon from the resolved installation.
-  wakeUpServiceFromLocation(logger, installLocation, isDaemon);
+  const child = wakeUpServiceFromLocation(logger, installLocation, isDaemon, foreground);
 
   // 4. Poll for the daemon to become available.
   for (let attempt = 1; attempt <= maxAttempts; attempt++) {
@@ -124,6 +130,22 @@ export async function findOrStartLlmster(
     if (serverStatus !== null) {
       logger.debug(`LM Studio daemon became available at port ${serverStatus}`);
       logger.debug(`package=${serverStatus.package}, version=${serverStatus.version}`);
+
+      if (foreground && child !== null) {
+        // Block until the foreground child exits so the caller's process
+        // supervisor can track the lifecycle.
+        await new Promise<void>((resolve, reject) => {
+          child.on("exit", (code, signal) => {
+            if (code === 0 || signal === "SIGTERM" || signal === "SIGINT") {
+              resolve();
+            } else {
+              reject(new Error(`LM Studio daemon exited with code ${code}, signal ${signal}`));
+            }
+          });
+          child.on("error", reject);
+        });
+      }
+
       return serverStatus;
     }
   }
@@ -215,7 +237,8 @@ function wakeUpServiceFromLocation(
   logger: LoggerInterface,
   installLocation: InstallLocation,
   isDaemon: boolean,
-) {
+  foreground: boolean = false,
+): ChildProcess | null {
   logger.info("Waking up LM Studio service...");
 
   const args: Array<string> = [];
@@ -237,6 +260,14 @@ function wakeUpServiceFromLocation(
   } as NodeJS.ProcessEnv;
 
   try {
+    if (foreground && process.platform !== "win32") {
+      // Foreground mode: keep the child attached so a process supervisor can
+      // own its lifecycle via signals / wait.
+      const child = spawn(path, args, { cwd, detached: false, stdio: "inherit", env });
+      logger.debug(`LM Studio daemon process spawned in foreground (PID ${child.pid}).`);
+      return child;
+    }
+
     if (process.platform === "win32" && isDaemon) {
       // On Windows + daemon, launch via PowerShell's Start-Process with a Hidden window style
       // to avoid opening a console window.
@@ -274,4 +305,6 @@ function wakeUpServiceFromLocation(
   } catch (e) {
     logger.debug("Failed to launch LM Studio daemon process:", e);
   }
+
+  return null;
 }
