diff --git a/music_assistant/controllers/config/dsp.py b/music_assistant/controllers/config/dsp.py
index a33c8eb73..5dca8813b 100644
--- a/music_assistant/controllers/config/dsp.py
+++ b/music_assistant/controllers/config/dsp.py
@@ -187,7 +187,7 @@ async def upload_dsp_ir(self, name: str, data: str) -> dict[str, Any]:
             # transcoding to wav both validates the upload is decodable audio and
             # gives afir a format it can always read back at playback time
             returncode, output = await check_output(
-                "ffmpeg",
+                "@ffmpeg@",
                 "-hide_banner",
                 "-loglevel",
                 "error",
diff --git a/music_assistant/controllers/streams/smart_fades/fades.py b/music_assistant/controllers/streams/smart_fades/fades.py
index 9a35a55dd..05f29df7a 100644
--- a/music_assistant/controllers/streams/smart_fades/fades.py
+++ b/music_assistant/controllers/streams/smart_fades/fades.py
@@ -242,7 +242,7 @@ def _mix_ffmpeg_args(self, pcm_format: AudioFormat, fadeout_read_fd: int) -> lis
             pcm_format.content_type.value,
         ]
         return [
-            "ffmpeg",
+            "@ffmpeg@",
             "-hide_banner",
             "-loglevel",
             "error",
diff --git a/music_assistant/helpers/audio.py b/music_assistant/helpers/audio.py
index ce3b5e580..702722cc4 100644
--- a/music_assistant/helpers/audio.py
+++ b/music_assistant/helpers/audio.py
@@ -157,7 +157,7 @@ async def strip_silence(
     :param pcm_format: AudioFormat of the audio data.
     :param reverse: If True, strip from end instead of beginning.
     """
-    args = ["ffmpeg", "-hide_banner", "-loglevel", "quiet"]
+    args = ["@ffmpeg@", "-hide_banner", "-loglevel", "quiet"]
     args += [
         "-acodec",
         pcm_format.content_type.name.lower(),
@@ -511,7 +511,7 @@ async def get_silence(
         return
     # use ffmpeg for all other encodings
     args = [
-        "ffmpeg",
+        "@ffmpeg",
         "-hide_banner",
         "-loglevel",
         "quiet",
diff --git a/music_assistant/helpers/ffmpeg.py b/music_assistant/helpers/ffmpeg.py
index 71fffbb99..371549269 100644
--- a/music_assistant/helpers/ffmpeg.py
+++ b/music_assistant/helpers/ffmpeg.py
@@ -518,7 +518,7 @@ def get_ffmpeg_args(
         extra_output_args = []
     # the binary plus the options that apply to the command as a whole
     global_args = [
-        "ffmpeg",
+        "@ffmpeg@",
         "-hide_banner",
         "-loglevel",
         loglevel,
@@ -689,7 +689,7 @@ async def check_ffmpeg_version() -> None:
     """Check that ffmpeg is present and usable, and cache the capabilities it reports."""
     # check for FFmpeg presence
     try:
-        returncode, output = await check_output("ffmpeg", "-version")
+        returncode, output = await check_output("@ffmpeg@", "-version")
     except FileNotFoundError:
         raise AudioError(
             "FFmpeg binary is missing from system. "
@@ -717,7 +717,7 @@ async def check_ffmpeg_version() -> None:
     # which builds from git report as e.g. "N-121037-g1234567". A probe that fails reads as
     # "not blocked", so the check stays in place. Drop this once every supported build
     # whitelists CMAF.
-    returncode, hls_options = await check_output("ffmpeg", "-hide_banner", "-h", "demuxer=hls")
+    returncode, hls_options = await check_output("@ffmpeg@", "-hide_banner", "-h", "demuxer=hls")
     cmaf_blocked = (
         returncode == 0 and b"extension_picky" in hls_options and b"cmfa" not in hls_options
     )
diff --git a/music_assistant/helpers/tags.py b/music_assistant/helpers/tags.py
index 87e37be5e..d1a17e742 100644
--- a/music_assistant/helpers/tags.py
+++ b/music_assistant/helpers/tags.py
@@ -697,7 +697,7 @@ def parse_tags(
     Input_file may be a (local) filename or URL accessible by ffmpeg.
     """
     args = (
-        "ffprobe",
+        "@ffprobe@",
         "-hide_banner",
         "-loglevel",
         "error",
@@ -764,7 +764,7 @@ def get_file_duration(input_file: str) -> float:
     NOT Async friendly.
     """
     args = (
-        "ffmpeg",
+        "@ffmpeg@",
         "-hide_banner",
         "-loglevel",
         "info",
@@ -1473,7 +1473,7 @@ async def get_embedded_image(input_file: str) -> bytes | None:
 
     # Use FFmpeg for all other cases (URLs, ID3 tags, Vorbis comments, etc.)
     args = [
-        "ffmpeg",
+        "@ffmpeg@",
         "-hide_banner",
         "-loglevel",
         "error",
diff --git a/music_assistant/providers/ai_radio/rendering.py b/music_assistant/providers/ai_radio/rendering.py
index 9f632931b..2b95433ad 100644
--- a/music_assistant/providers/ai_radio/rendering.py
+++ b/music_assistant/providers/ai_radio/rendering.py
@@ -408,7 +408,7 @@ async def _measure_loudness(self, path: str) -> float | None:
         """Return the integrated loudness of the given audio in LUFS, or None when it fails."""
         try:
             returncode, output = await check_output(
-                "ffmpeg",
+                "@ffmpeg@",
                 "-hide_banner",
                 "-nostats",
                 "-i",
diff --git a/music_assistant/providers/ambient_sounds/__init__.py b/music_assistant/providers/ambient_sounds/__init__.py
index 2332005d4..84d13d84f 100644
--- a/music_assistant/providers/ambient_sounds/__init__.py
+++ b/music_assistant/providers/ambient_sounds/__init__.py
@@ -330,7 +330,7 @@ async def _ensure_rendered(self, preset_id: str, preset: AmbientPreset) -> str:
             # leave a partial file behind at the final path
             tmp_path = f"{file_path}.tmp"
             returncode, output = await check_output(
-                "ffmpeg",
+                "@ffmpeg@",
                 "-hide_banner",
                 "-loglevel",
                 "error",
diff --git a/scripts/perf/run_benchmark.py b/scripts/perf/run_benchmark.py
index beb242c0a..b98329e31 100644
--- a/scripts/perf/run_benchmark.py
+++ b/scripts/perf/run_benchmark.py
@@ -883,7 +883,7 @@ def _git(*args: str) -> str:
                     cpu = line.split(":", 1)[1].strip()
                     break
     ffmpeg = subprocess.run(
-        ["ffmpeg", "-version"],  # noqa: S607
+        ["@ffmpeg@", "-version"],  # noqa: S607
         capture_output=True,
         text=True,
         check=False,
diff --git a/tests/controllers/config/test_dsp.py b/tests/controllers/config/test_dsp.py
index d5bf3f38b..a9413c2cc 100644
--- a/tests/controllers/config/test_dsp.py
+++ b/tests/controllers/config/test_dsp.py
@@ -176,7 +176,7 @@ def _wav_bytes(tmp_path: Path, channels: int = 2, duration: float = 0.1) -> byte
     wav_path = tmp_path / f"source_{channels}ch_{duration}s.wav"
     subprocess.run(  # noqa: S603
         [  # noqa: S607
-            "ffmpeg",
+            "@ffmpeg@",
             "-y",
             "-f",
             "lavfi",
@@ -197,7 +197,7 @@ def _silent_flac_bytes(tmp_path: Path, duration: float) -> bytes:
     flac_path = tmp_path / f"silence_{duration}s.flac"
     subprocess.run(  # noqa: S603
         [  # noqa: S607
-            "ffmpeg",
+            "@ffmpeg@",
             "-y",
             "-f",
             "lavfi",
diff --git a/tests/helpers/test_ffmpeg.py b/tests/helpers/test_ffmpeg.py
index fdf723028..a2f1b9aa8 100644
--- a/tests/helpers/test_ffmpeg.py
+++ b/tests/helpers/test_ffmpeg.py
@@ -127,7 +127,7 @@ def test_multichannel_pcm_folds_down_without_stretching(tmp_path: Path) -> None:
     # only the rear channels carry a tone: a downmix that drops them yields silence
     subprocess.run(  # noqa: S603
         [  # noqa: S607
-            "ffmpeg",
+            "@ffmpeg@",
             "-y",
             "-f",
             "lavfi",
@@ -163,7 +163,7 @@ def test_multichannel_pcm_folds_down_without_stretching(tmp_path: Path) -> None:
 
     duration = subprocess.run(  # noqa: S603
         [  # noqa: S607
-            "ffprobe",
+            "@ffprobe@",
             "-v",
             "error",
             "-show_entries",
@@ -451,7 +451,7 @@ def overlay_file(tmp_path: Path) -> Path:
     """Generate a 1 second mono sine-tone wav file to use as overlay source."""
     overlay_path = tmp_path / "overlay.wav"
     subprocess.run(  # noqa: S603
-        ["ffmpeg", "-f", "lavfi", "-i", "sine=frequency=440:duration=1", str(overlay_path)],  # noqa: S607
+        ["@ffmpeg@", "-f", "lavfi", "-i", "sine=frequency=440:duration=1", str(overlay_path)],  # noqa: S607
         check=True,
         capture_output=True,
     )
@@ -464,7 +464,7 @@ def overlay_file_stereo(tmp_path: Path) -> Path:
     overlay_path = tmp_path / "overlay_stereo.wav"
     subprocess.run(  # noqa: S603
         [  # noqa: S607
-            "ffmpeg",
+            "@ffmpeg@",
             "-f",
             "lavfi",
             "-i",
@@ -485,7 +485,7 @@ def overlay_file_wide_stereo(tmp_path: Path) -> Path:
     overlay_path = tmp_path / "overlay_wide.wav"
     subprocess.run(  # noqa: S603
         [  # noqa: S607
-            "ffmpeg",
+            "@ffmpeg@",
             "-f",
             "lavfi",
             "-i",
@@ -506,7 +506,7 @@ def overlay_file_with_silent_intro(tmp_path: Path) -> Path:
     overlay_path = tmp_path / "overlay_silent_intro.wav"
     subprocess.run(  # noqa: S603
         [  # noqa: S607
-            "ffmpeg",
+            "@ffmpeg@",
             "-f",
             "lavfi",
             "-i",
@@ -1067,7 +1067,7 @@ def _rms_db(path: Path) -> float:
     """Return the overall RMS level of an audio file in dB via ffmpeg astats."""
     output = subprocess.run(  # noqa: S603
         [  # noqa: S607
-            "ffmpeg",
+            "@ffmpeg@",
             "-hide_banner",
             "-nostats",
             "-i",
@@ -1095,7 +1095,7 @@ def test_filtergraph_complex_runs_in_ffmpeg(tmp_path: Path) -> None:
     out = tmp_path / "out.wav"
     subprocess.run(  # noqa: S603
         [  # noqa: S607
-            "ffmpeg",
+            "@ffmpeg@",
             "-y",
             "-f",
             "lavfi",
@@ -1111,7 +1111,7 @@ def test_filtergraph_complex_runs_in_ffmpeg(tmp_path: Path) -> None:
     # a single-sample impulse is the identity IR: convolving with it returns the input
     subprocess.run(  # noqa: S603
         [  # noqa: S607
-            "ffmpeg",
+            "@ffmpeg@",
             "-y",
             "-f",
             "lavfi",
@@ -1127,7 +1127,7 @@ def test_filtergraph_complex_runs_in_ffmpeg(tmp_path: Path) -> None:
     )
     result = subprocess.run(  # noqa: S603
         [  # noqa: S607
-            "ffmpeg",
+            "@ffmpeg@",
             "-hide_banner",
             "-loglevel",
             "error",
@@ -1154,7 +1154,7 @@ def test_mono_source_keeps_its_level_when_widened_to_stereo(tmp_path: Path) -> N
     out = tmp_path / "stereo.wav"
     subprocess.run(  # noqa: S603
         [  # noqa: S607
-            "ffmpeg",
+            "@ffmpeg@",
             "-y",
             "-f",
             "lavfi",
diff --git a/tests/helpers/test_images.py b/tests/helpers/test_images.py
index dfc436bdc..98d46e607 100644
--- a/tests/helpers/test_images.py
+++ b/tests/helpers/test_images.py
@@ -570,7 +570,7 @@ def _create_mp3_with_cover(track_path: str, cover_path: str) -> None:
     """Create a short mp3 file with the given image embedded as cover art."""
     subprocess.run(  # noqa: S603
         [  # noqa: S607
-            "ffmpeg",
+            "@ffmpeg@",
             "-y",
             "-hide_banner",
             "-loglevel",
diff --git a/tests/helpers/test_tags.py b/tests/helpers/test_tags.py
index e21c760ea..ae3eb0493 100644
--- a/tests/helpers/test_tags.py
+++ b/tests/helpers/test_tags.py
@@ -50,7 +50,7 @@ def test_parse_tags_reports_actionable_ffprobe_error(
     """Test that tag parsing reports a useful FFprobe failure."""
     process_error = subprocess.CalledProcessError(
         returncode=1,
-        cmd=("ffprobe",),
+        cmd=("@ffprobe@",),
         output=b'{"error":{"code":-1,"string":"Unknown error occurred"}}',
         stderr=stderr,
     )
