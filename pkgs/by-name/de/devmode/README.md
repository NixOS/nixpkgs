# `devmode`

`devmode` is a daemon that:

1. watches the manual's source for changes and when they occur — rebuilds
1. HTTP serves the manual, injecting a script that triggers reload on changes
1. opens the manual in the default browser
