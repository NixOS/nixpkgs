diff --git a/cmd/launch/launch.go b/cmd/launch/launch.go
index dd65466..994dc3a 100644
--- a/cmd/launch/launch.go
+++ b/cmd/launch/launch.go
@@ -459,11 +459,19 @@ func (c *launcherClient) launchSingleIntegration(ctx context.Context, name strin
 	}
 
 	if needsConfigure {
-		selected, err := c.selectSingleModelWithSelector(ctx, fmt.Sprintf("Select model for %s:", runner), target, DefaultSingleSelector)
-		if err != nil {
-			return err
+		if c.policy.Confirm == LaunchConfirmAutoApprove && target != "" {
+			// Non-interactive auto-approve with a model already specified:
+			// skip the interactive picker and use the target directly.
+			if err := c.ensureModelsReady(ctx, []string{target}); err != nil {
+				return err
+			}
+		} else {
+			selected, err := c.selectSingleModelWithSelector(ctx, fmt.Sprintf("Select model for %s:", runner), target, DefaultSingleSelector)
+			if err != nil {
+				return err
+			}
+			target = selected
 		}
-		target = selected
 	} else if err := c.ensureModelsReady(ctx, []string{target}); err != nil {
 		return err
 	}
@@ -485,11 +493,19 @@ func (c *launcherClient) launchEditorIntegration(ctx context.Context, name strin
 	models, needsConfigure := c.resolveEditorLaunchModels(ctx, saved, req)
 
 	if needsConfigure {
-		selected, err := c.selectMultiModelsForIntegration(ctx, runner, models)
-		if err != nil {
-			return err
+		if c.policy.Confirm == LaunchConfirmAutoApprove && len(models) > 0 {
+			// Non-interactive auto-approve with models already resolved:
+			// skip the interactive picker and use the resolved models directly.
+			if err := c.ensureModelsReady(ctx, models); err != nil {
+				return err
+			}
+		} else {
+			selected, err := c.selectMultiModelsForIntegration(ctx, runner, models)
+			if err != nil {
+				return err
+			}
+			models = selected
 		}
-		models = selected
 	} else if len(models) > 0 {
 		if err := c.ensureModelsReady(ctx, models[:1]); err != nil {
 			return err
diff --git a/cmd/launch/launch_test.go b/cmd/launch/launch_test.go
index a9bd53c..ca2de6f 100644
--- a/cmd/launch/launch_test.go
+++ b/cmd/launch/launch_test.go
@@ -1966,6 +1966,112 @@ func TestLaunchIntegration_HeadlessSelectorFlowFailsWithoutPrompt(t *testing.T)
 	}
 }
 
+func TestLaunchIntegration_EditorAutoApproveSkipsPickerWithModel(t *testing.T) {
+	tmpDir := t.TempDir()
+	setLaunchTestHome(t, tmpDir)
+	withLauncherHooks(t)
+	withInteractiveSession(t, false)
+
+	binDir := t.TempDir()
+	writeFakeBinary(t, binDir, "droid")
+	t.Setenv("PATH", binDir)
+
+	editor := &launcherEditorRunner{paths: []string{"/tmp/settings.json"}}
+	withIntegrationOverride(t, "droid", editor)
+
+	DefaultMultiSelector = func(title string, items []ModelItem, preChecked []string) ([]string, error) {
+		t.Fatal("multi selector should not be called in auto-approve mode with model override")
+		return nil, nil
+	}
+
+	DefaultConfirmPrompt = func(prompt string, options ConfirmOptions) (bool, error) {
+		return true, nil
+	}
+
+	srv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
+		switch r.URL.Path {
+		case "/api/tags":
+			fmt.Fprint(w, `{"models":[{"name":"llama3.2"},{"name":"qwen3:8b"}]}`)
+		case "/api/show":
+			var req apiShowRequest
+			_ = json.NewDecoder(r.Body).Decode(&req)
+			fmt.Fprintf(w, `{"model":%q}`, req.Model)
+		default:
+			http.NotFound(w, r)
+		}
+	}))
+	defer srv.Close()
+	t.Setenv("OLLAMA_HOST", srv.URL)
+
+	policy := LaunchPolicy{
+		Confirm:      LaunchConfirmAutoApprove,
+		MissingModel: LaunchMissingModelAutoPull,
+	}
+	err := LaunchIntegration(context.Background(), IntegrationLaunchRequest{
+		Name:           "droid",
+		ModelOverride:  "llama3.2",
+		ForceConfigure: true,
+		Policy:         &policy,
+	})
+	if err != nil {
+		t.Fatalf("LaunchIntegration returned error: %v", err)
+	}
+	if editor.ranModel != "llama3.2" {
+		t.Fatalf("expected launch with model llama3.2, got %q", editor.ranModel)
+	}
+}
+
+func TestLaunchIntegration_SingleAutoApproveSkipsPickerWithModel(t *testing.T) {
+	tmpDir := t.TempDir()
+	setLaunchTestHome(t, tmpDir)
+	withLauncherHooks(t)
+	withInteractiveSession(t, false)
+
+	binDir := t.TempDir()
+	writeFakeBinary(t, binDir, "claude")
+	t.Setenv("PATH", binDir)
+
+	runner := &launcherSingleRunner{}
+	withIntegrationOverride(t, "claude", runner)
+
+	DefaultSingleSelector = func(title string, items []ModelItem, current string) (string, error) {
+		t.Fatal("single selector should not be called in auto-approve mode with model override")
+		return "", nil
+	}
+
+	srv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
+		switch r.URL.Path {
+		case "/api/tags":
+			fmt.Fprint(w, `{"models":[{"name":"llama3.2"}]}`)
+		case "/api/show":
+			var req apiShowRequest
+			_ = json.NewDecoder(r.Body).Decode(&req)
+			fmt.Fprintf(w, `{"model":%q}`, req.Model)
+		default:
+			http.NotFound(w, r)
+		}
+	}))
+	defer srv.Close()
+	t.Setenv("OLLAMA_HOST", srv.URL)
+
+	policy := LaunchPolicy{
+		Confirm:      LaunchConfirmAutoApprove,
+		MissingModel: LaunchMissingModelAutoPull,
+	}
+	err := LaunchIntegration(context.Background(), IntegrationLaunchRequest{
+		Name:           "claude",
+		ModelOverride:  "llama3.2",
+		ForceConfigure: true,
+		Policy:         &policy,
+	})
+	if err != nil {
+		t.Fatalf("LaunchIntegration returned error: %v", err)
+	}
+	if runner.ranModel != "llama3.2" {
+		t.Fatalf("expected launch with model llama3.2, got %q", runner.ranModel)
+	}
+}
+
 type apiShowRequest struct {
 	Model string `json:"model"`
 }
