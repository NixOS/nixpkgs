diff --git a/XLink.cmake b/XLink.cmake
index 8d55186..3a65233 100644
--- a/XLink.cmake
+++ b/XLink.cmake
@@ -19,8 +19,8 @@ if(WIN32)
 else()
     find_package(Threads REQUIRED)
 
-    find_path(LIBUSB_INCLUDE_DIR NAMES libusb.h PATH_SUFFIXES "include" "libusb" "libusb-1.0")
-    find_library(LIBUSB_LIBRARY NAMES usb-1.0 PATH_SUFFIXES "lib")
+    set(LIBUSB_INCLUDE_DIR "@LIBUSB_INCLUDE_DIR@")
+    set(LIBUSB_LIBRARY "@LIBUSB_LIBRARY@")
 
     if(NOT LIBUSB_INCLUDE_DIR OR NOT LIBUSB_LIBRARY)
         message(FATAL_ERROR "libusb is required")
