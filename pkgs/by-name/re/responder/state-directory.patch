diff --git a/certs/gen-self-signed-cert.sh b/certs/gen-self-signed-cert.sh
index aeb53a7..ecadd2b 100755
--- a/certs/gen-self-signed-cert.sh
+++ b/certs/gen-self-signed-cert.sh
@@ -1,3 +1,5 @@
 #!/bin/bash
-openssl genrsa -out certs/responder.key 2048
-openssl req -new -x509 -days 3650 -key certs/responder.key -out certs/responder.crt -subj "/"
+set -eu
+
+openssl genrsa -out "$1/responder.key" 2048
+openssl req -new -x509 -days 3650 -key "$1/responder.key" -out "$1/responder.crt" -subj "/"
diff --git a/settings.py b/settings.py
index c48310e..af78563 100755
--- a/settings.py
+++ b/settings.py
@@ -29,6 +29,7 @@ class Settings:
 	
 	def __init__(self):
 		self.ResponderPATH = os.path.dirname(__file__)
+		self.StateDir = os.environ.get('RESPONDER_STATE_DIR', self.ResponderPATH)
 		self.Bind_To = '0.0.0.0'
 
 	def __str__(self):
@@ -142,13 +143,12 @@ class Settings:
 		self.SNMP_On_Off     = self.toBool(config.get('Responder Core', 'SNMP'))
 
 		# Db File
-		self.DatabaseFile    = os.path.join(self.ResponderPATH, config.get('Responder Core', 'Database'))
+		self.DatabaseFile    = os.path.join(self.StateDir, config.get('Responder Core', 'Database'))
 
 		# Log Files
-		self.LogDir = os.path.join(self.ResponderPATH, 'logs')
+		self.LogDir = os.path.join(self.StateDir, 'logs')
 
-		if not os.path.exists(self.LogDir):
-			os.mkdir(self.LogDir)
+		os.makedirs(self.LogDir, mode=0o700, exist_ok=True)
 
 		self.SessionLogFile      = os.path.join(self.LogDir, config.get('Responder Core', 'SessionLog'))
 		self.PoisonersLogFile    = os.path.join(self.LogDir, config.get('Responder Core', 'PoisonersLog'))
@@ -308,4 +308,4 @@ class Settings:
 		# SSL Options
-		self.SSLKey  = config.get('HTTPS Server', 'SSLKey')
-		self.SSLCert = config.get('HTTPS Server', 'SSLCert')
+		self.SSLKey  = os.path.join(self.StateDir, config.get('HTTPS Server', 'SSLKey'))
+		self.SSLCert = os.path.join(self.StateDir, config.get('HTTPS Server', 'SSLCert'))
 
@@ -370,12 +370,15 @@ class Settings:
 		self.AnalyzeLogger = logging.getLogger('Analyze Log')
 		self.AnalyzeLogger.addHandler(ALog_Handler)
 		
-		# First time Responder run?
-		if os.path.isfile(self.ResponderPATH+'/Responder.db'):
-			pass
-		else:
-			#If it's the first time, generate SSL certs for this Responder session and send openssl output to /dev/null
-			Certs = os.system(self.ResponderPATH+"/certs/gen-self-signed-cert.sh >/dev/null 2>&1")
+		# Generate persistent SSL certificates on the first run.
+		if not os.path.isfile(self.SSLCert) or not os.path.isfile(self.SSLKey):
+			os.makedirs(os.path.dirname(self.SSLCert), mode=0o700, exist_ok=True)
+			subprocess.run(
+				[os.path.join(self.ResponderPATH, 'certs/gen-self-signed-cert.sh'), os.path.dirname(self.SSLCert)],
+				check=True,
+				stdout=subprocess.DEVNULL,
+				stderr=subprocess.DEVNULL,
+			)
 		
 		try:
 			NetworkCard = subprocess.check_output(["ifconfig", "-a"])
diff --git a/utils.py b/utils.py
index eec962f..5ab8633 100644
--- a/utils.py
+++ b/utils.py
@@ -440,3 +440,3 @@ def SaveToDb(result):
 		(count,) = res.fetchone()
-		logfile = os.path.join(settings.Config.ResponderPATH, 'logs', fname)
+		logfile = os.path.join(settings.Config.LogDir, fname)
 
