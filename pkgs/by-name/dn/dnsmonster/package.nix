{
  lib,
  stdenv,
  buildGoModule,
  fetchFromGitHub,
  libpcap,
}:

buildGoModule (finalAttrs: {
  pname = "dnsmonster";
  version = "1.2.5";

  src = fetchFromGitHub {
    owner = "FenkoHQ";
    repo = "dnsmonster";
    tag = "v${finalAttrs.version}";
    hash = "sha256-Ae7SzImNHOOpaaVLFHdfLrwGhaHkvZBt+s/sRoHYwzk=";
  };

  # the pinned github.com/bytedance/sonic@v1.14.2 does not build with go 1.26.
  # this has been resolved upstream, but not yet released, so we patch in an
  # update in the interim. remove on next update.
  #
  # patch generated by checking out dnsmonster at 1.2.5 and then running
  # `go get github.com/bytedance/sonic@v1.15.3`.
  patches = [ ./go126-update-sonic-dependency.patch ];

  vendorHash = "sha256-QhM3AgrPcRyk16QwdqimX33Ta/xRdReHFyEVjKMeLIQ=";

  buildInputs = [ libpcap ];

  ldflags = [
    "-s"
    "-w"
    "-X=github.com/mosajjal/dnsmonster/util.releaseVersion=${finalAttrs.version}"
  ];

  meta = {
    description = "Passive DNS Capture and Monitoring Toolkit";
    homepage = "https://github.com/FenkoHQ/dnsmonster";
    changelog = "https://github.com/FenkoHQ/dnsmonster/releases/tag/${finalAttrs.src.tag}";
    license = lib.licenses.gpl2Only;
    maintainers = with lib.maintainers; [ fab ];
    broken = stdenv.hostPlatform.isDarwin;
    mainProgram = "dnsmonster";
  };
})
