From 90dda2c728007efbde8ce2cedac4efee44138b4d Mon Sep 17 00:00:00 2001
From: phanirithvij <phanirithvij2000@gmail.com>
Date: Wed, 24 Dec 2025 10:46:43 +0530
Subject: [PATCH 1/3] try debug prune bug

Signed-off-by: phanirithvij <phanirithvij2000@gmail.com>
---
 src/goupile/server/goupile.cc | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/src/goupile/server/goupile.cc b/src/goupile/server/goupile.cc
index 94085882e..27de3deb1 100644
--- a/src/goupile/server/goupile.cc
+++ b/src/goupile/server/goupile.cc
@@ -1204,7 +1204,7 @@ For help about those commands, type: %!..+%1 command --help%!0)"),
         const char *tmp_filename2 = CreateUniqueFile(gp_config.instances_directory, "", ".tmp", &temp_alloc);
         if (!tmp_filename2)
             return 1;
-        K_DEFER { UnlinkFile(tmp_filename2); };
+        //K_DEFER { UnlinkFile(tmp_filename2); };
 
         if (RenameFile(tmp_filename1, tmp_filename2, (int)RenameFlag::Overwrite) != RenameResult::Success)
             return 1;
@@ -1287,8 +1287,9 @@ For help about those commands, type: %!..+%1 command --help%!0)"),
         LogInfo("Periodic timer set to %1 s", FmtDouble((double)timeout / 1000.0, 1));
 
         while (run) {
-            if (!PerformDuties(first))
+            /*if (!PerformDuties(first))
                 return 1;
+            */
 
             WaitResult ret = WaitEvents(timeout);
 
-- 
2.51.2

