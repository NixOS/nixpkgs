diff --git i/src-tauri/qf_api/src/endpoints/analytics.rs w/src-tauri/qf_api/src/endpoints/analytics.rs
index b7ea607..10fb0bf 100644
--- i/src-tauri/qf_api/src/endpoints/analytics.rs
+++ w/src-tauri/qf_api/src/endpoints/analytics.rs
@@ -51,6 +51,7 @@ impl AnalyticsRoute {
     }
 
     pub fn start(self: Arc<Self>) -> Result<(), ApiError> {
+        return Ok(());
         let mut stop = self._stop.lock().unwrap();
         *stop = false;
         tokio::spawn({
diff --git i/src/contexts/app.context.tsx w/src/contexts/app.context.tsx
index 26d09d0..4cc687f 100644
--- i/src/contexts/app.context.tsx
+++ w/src/contexts/app.context.tsx
@@ -116,7 +116,7 @@ export function AppContextProvider({ children }: AppContextProviderProps) {
     });
   };
 
-  const checkForTosUpdates = async (info: TauriTypes.AppInfo) => {
+  const checkForTosUpdates = async (_info: TauriTypes.AppInfo) => {
     const resourcePath = await resolveResource("resources/tos.md");
     const context = await readTextFile(resourcePath);
     // Get Text Between <ID</ID>
@@ -124,7 +124,7 @@ export function AppContextProvider({ children }: AppContextProviderProps) {
     const end = context.indexOf("</ID>");
     const id = context.substring(start, end);
 
-    if (id == info?.tos_uuid) return;
+    if (true) return;
     const modalId = modals.open({
       title: useTranslateComponent("modals.tos.title", { version: id }),
       withCloseButton: false,
