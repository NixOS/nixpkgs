diff --git a/setup.py b/setup.py
index e96f58405fe3..d33b80a7fd0a 100644
--- a/setup.py
+++ b/setup.py
@@ -43,11 +43,15 @@ def copy_prebuilt_C_lib(src_dir, dest_dir, dest_build_dir):
             s = os.path.join(dirpath, item)
             if item.endswith(".h"):
                 d = os.path.join(dest_dir, item)
+                if os.path.isfile(d):
+                    continue
                 shutil.copy2(s, d)
                 files_copied += 1
             if item.endswith(lib_ext) or item.endswith('.metallib'):
                 s = os.path.join(dirpath, item)
                 d = os.path.join(dest_build_dir, item)
+                if os.path.isfile(d):
+                    continue
                 shutil.copy2(s, d)
                 files_copied += 1
     
