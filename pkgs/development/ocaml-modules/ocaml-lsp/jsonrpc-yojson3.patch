--- a/jsonrpc/src/import.ml
+++ b/jsonrpc/src/import.ml
@@ -17,8 +17,6 @@ module Json = struct
     | `List of t list
     | `Null
     | `String of string
-    | `Tuple of t list
-    | `Variant of string * t option
     ]

   exception Of_json of (string * t)
--- a/jsonrpc/src/jsonrpc.mli
+++ b/jsonrpc/src/jsonrpc.mli
@@ -10,8 +10,6 @@ module Json : sig
     | `List of t list
     | `Null
     | `String of string
-    | `Tuple of t list
-    | `Variant of string * t option
     ]

   (** Raised when conversions from json fail *)
