diff --git a/src-tauri/src/fix_env.rs b/src-tauri/src/fix_env.rs
index 856bf8f..85ab303 100644
--- a/src-tauri/src/fix_env.rs
+++ b/src-tauri/src/fix_env.rs
@@ -42,10 +42,10 @@ pub fn fix_env(var_name: &str) -> Result<(), Error> {
 
         if out.status.success() {
             let stdout = String::from_utf8_lossy(&out.stdout).into_owned();
-            let cleaned = &strip_ansi_escapes::strip(stdout.trim())?;
+            let cleaned = &strip_ansi_escapes::strip(stdout)?;
             let value = String::from_utf8_lossy(cleaned);
             unsafe {
-                std::env::set_var(var_name, value.as_ref());
+                std::env::set_var(var_name, value.trim_end_matches(['\r', '\n']));
             }
             Ok(())
         } else {
