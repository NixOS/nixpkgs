diff --git a/Cargo.lock b/Cargo.lock
index 88b8a8d..8e0d601 100644
--- a/Cargo.lock
+++ b/Cargo.lock
@@ -1,5 +1,7 @@
 # This file is automatically @generated by Cargo.
 # It is not intended for manual editing.
+version = 3
+
 [[package]]
 name = "addr2line"
 version = "0.15.1"
@@ -855,7 +857,6 @@ source = "registry+https://github.com/rust-lang/crates.io-index"
 checksum = "15d246e3d21b63ad94a763be34e4d528402ac50705eddfb27734c7f1a99db44e"
 dependencies = [
  "bindgen",
- "cc",
  "pkg-config",
 ]
 
diff --git a/Cargo.toml b/Cargo.toml
index fc3dcde..f273bd1 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -66,7 +66,7 @@ isolang = { version = "1", features = ["serde_serialize"] }
 url = { version = "2", features = ["serde"] }
 
 # dictionary lookup with affixes
-hunspell-rs = { version = "0.3", optional = true }
+hunspell-rs = { version = "0.3", optional = true, default-features = false }
 fd-lock = { version = "2", optional = true }
 
 # full grammar check, but also tokenization and disambiguation
@@ -83,7 +83,7 @@ serde_plain = "0.3"
 default = ["hunspell", "nlprules"]
 
 # hunspell uses the segmenter provided by nlprules
-hunspell = ["hunspell-rs/bundled", "fd-lock", "nlprules"]
+hunspell = ["hunspell-rs", "fd-lock", "nlprules"]
 nlprules = ["nlprule/regex-fancy", "nlprule-build"]
 
 all = ["hunspell", "nlprules"]
