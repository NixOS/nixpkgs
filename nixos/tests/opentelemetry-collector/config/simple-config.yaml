exporters:
  debug:
    verbosity: detailed

receivers:
  otlp:
    protocols:
      http:
        endpoint: 0.0.0.0:4318

service:
  pipelines:
    logs:
      receivers:
        - otlp
      exporters:
        - debug
