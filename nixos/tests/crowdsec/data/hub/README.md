# Update `.index.json`

1. Clone `https://github.com/TornaxO7/hub`
2. Switch to branch `crowdsec-test` (`git switch crowdsec-test`)
3. Apply changes to the files
4. Execute `uv run src/cshub/mkindex.py`
5. Copy the generated `.index.json`
