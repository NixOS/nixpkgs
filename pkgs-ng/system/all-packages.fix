{system}: rec
  { stdenv = (import ../stdenv)
      { system = system
      }

  , fetchurl = (import ../fetchurl)
      { stdenv = stdenv
      }

  , aterm = (import ../aterm)
      { fetchurl = fetchurl
      , stdenv = stdenv
      }
  }